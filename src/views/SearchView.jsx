import React from 'react';
import { Box } from '@mui/material';

const SearchView = () => {
  <Box></Box>// Contenido de tu componente
};

export default SearchView; // Asegúrate de exportar el componente como default
