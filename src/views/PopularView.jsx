import React from 'react';
import { Box } from '@mui/material';


const PopularView = () => {
  <Box></Box>// Contenido de tu componente
};

export default PopularView; // Asegúrate de exportar el componente como default
